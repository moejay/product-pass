{
  "name": "product-pass",
  "version": "0.5.0",
  "private": true,
  "description": "Capture visual website feedback and publish approved issue drafts to GitHub.",
  "license": "MIT",
  "homepage": "https://moejay.github.io/product-pass/",
  "repository": "github:moejay/product-pass",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
